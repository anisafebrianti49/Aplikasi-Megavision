<?xml version="1.0" encoding="utf-8"?>
<!-- 1. Pasang background gradasi pada root agar dasar aplikasi langsung berwarna biru -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_blue">

    <!-- Kontainer fragment dibuat transparan agar warna gradasi dari dasar bisa tembus -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"
        android:background="@android:color/transparent" />

    <!-- 2. Perbarui warna itemIconTint dan itemTextColor menjadi BIRU untuk membuang warna ungu -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        app:itemIconTint="#1A60D1"
        app:itemTextColor="#1A60D1"
        app:menu="@menu/bottom_nav_menu" />
</RelativeLayout>